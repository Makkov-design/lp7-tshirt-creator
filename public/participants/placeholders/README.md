# Participant Placeholders

Placeholder portraits live here until real photos are added.

MVP behavior:
- show initials-based placeholder when no real photo exists;
- keep the same card layout so adding photos later does not change the flow;
- never block the survey because a photo file is missing.
